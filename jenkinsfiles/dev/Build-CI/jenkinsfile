pipeline {
    agent {
        label 'EC2-Agent'
    }

    environment {
        // Version and tag variables
        APP_VERSION = 'v1.2.0'
        BUILD_TAG = "${APP_VERSION}-${BUILD_NUMBER}"

        // Node environment
        NODE_ENV = 'development'
    }

    options {
        timeout(time: 30, unit: 'MINUTES')
        disableConcurrentBuilds()
        ansiColor('xterm')
    }

    stages {
        stage('Welcome Message') {
            steps {
                script {
                    echo "Welcome to the CI Pipeline! Building version ${BUILD_TAG}..."
                }
            }
        }

        stage('Install Dependencies') {
            steps {
                script {
                    try {
                        sh 'npm ci'
                    } catch (Exception e) {
                        currentBuild.result = 'FAILURE'
                        error "Failed to install dependencies: ${e.message}"
                    }
                }
            }
        }

        stage('Package Artifact') {  // Renamed stage for clarity
            steps {
                script {
                    // Create deployment package
                    sh '''
                        # Create a tarball of the necessary files
                        tar -czf backend-app-${BUILD_TAG}.tar.gz \
                            src/ \
                            package.json \
                            package-lock.json \
                            app.js \
                            server.js
                    '''
                    
                    // Archive the build artifact
                    archiveArtifacts artifacts: "backend-app-${BUILD_TAG}.tar.gz", fingerprint: true
                }
            }
        }
    }

    post {
        always {
            script {
                try {
                    cleanWs()
                } catch (Exception e) {
                    echo "Cleanup failed: ${e.message}"
                }
            }
        }
        success {
            echo "Pipeline completed successfully for version ${BUILD_TAG}!"
        }
        failure {
            echo "Pipeline failed for version ${BUILD_TAG}! Please check the logs for details."
        }
    }
}
